# Routine

SwiftUI iOS app (Xcode). Open **`ITGirl/ITGirl.xcodeproj`** in Xcode.

This repository was split from the Notepad Pro monorepo; sources were imported from the last revision that contained the full `ITGirl/` tree (commit `94a7ea8` on the former layout).

The Xcode product and module folder are still named **ITGirl** inside the project; rename in Xcode if you want the folder name to match “Routine” everywhere.
